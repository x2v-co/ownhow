{
  "name": "ownhow",
  "version": "0.2.0",
  "description": "Local-first governance and evolution for personal AI methods",
  "homepage": "https://ownhow.dev",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/x2v-co/ownhow.git"
  },
  "bugs": {
    "url": "https://github.com/x2v-co/ownhow/issues"
  },
  "type": "module",
  "bin": {
    "ownhow": "./src/cli.js"
  },
  "files": [
    "src/",
    "README.md",
    "SECURITY.md",
    "LICENSE"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "check": "node --check src/cli.js && node --check src/scanner.js && node --check src/analyzer.js && node --check src/resolver.js && node --check src/store.js",
    "test": "node --test",
    "verify": "npm run check && npm test"
  },
  "license": "Apache-2.0"
}
